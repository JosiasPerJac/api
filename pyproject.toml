[project]
name = "api"
version = "0.1.0"
description = "Mi API con FastAPI"
dependencies = [
    "fastapi[standard]",
    "pydantic",
    "python-dotenv",
    "supabase",
    "requests"
]

[tool.fastapi]
entrypoint = "main:app"