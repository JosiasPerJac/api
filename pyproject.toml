[project]
name = "api"
version = "0.1.0"
description = "Mi API con FastAPI"
dependencies = [
    "fastapi[standard]",
    "pydantic",
    "python-dotenv",
    "supabase"
]

[tool.fastapi]
entrypoint = "main:app"